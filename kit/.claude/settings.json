{
  "//": "Universal Agent Kit defaults for Claude Code. Grants read/glob/grep + edit/write + git with acceptEdits. Build/test/run are NOT granted - see //allow-examples and add the ones you trust. Review and tighten `allow` before adopting. `agent` names the default subagent from .claude/agents/.",
  "agent": "rd-lead",
  "//hooks-example": [
    "A command ladder documented in CLAUDE.md alone measured 0 uses of the cheapest tier over 434 invocations.",
    "Routing is decided when the user types, so nudge there. Add a hooks block and point it at your script:",
    "\"hooks\": { \"UserPromptSubmit\": [ { \"hooks\": [ { \"type\": \"command\", \"command\": \"<YOUR_NUDGE_SCRIPT>\" } ] } ] }",
    "The script: match the prompt against a short, high-precision micro-task pattern list, veto on a real-work",
    "list, drop anything past a length ceiling, print one line naming /quick and /fix, and ALWAYS exit 0.",
    "Advisory only - a hook that refuses a prompt costs more on one false fire than it saves on many hits.",
    "Left as an example, not wired: a hooks entry pointing at a script you have not written yet fails on every prompt."
  ],
  "permissions": {
    "allow": [
      "Read(**)",
      "Glob(**)",
      "Grep(**)",
      "Edit(**)",
      "Write(**)",
      "Bash(git *)"
    ],
    "//allow-examples": [
      "Add your build/test/run commands so the agent does not prompt, e.g.:",
      "Bash(npm run *)",
      "Bash(pnpm *)",
      "Bash(make *)",
      "Bash(./gradlew *)",
      "Bash(cargo *)",
      "Bash(go *)"
    ],
    "defaultMode": "acceptEdits"
  }
}
